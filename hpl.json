{
  "variables": {
  },
    "builders": [{
        "type": "openstack",
        "ssh_username": "ubuntu",
        "networks": ["{{user `network`}}"],
        "floating_ip_network": "{{user `floating_network`}}",
        "image_name": "{{user `hpl_image`}}",
        "source_image_name": "{{user `base_image`}}",
        "flavor": "{{user `flavor`}}"
    }],
    "provisioners": [{
       "type": "ansible",
      "playbook_file": "./customize.yml"
    }]
}
